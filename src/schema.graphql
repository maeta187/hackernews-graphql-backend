# GraphQLのスキーマー
type Link {
  id: ID!
  description: String!
  url: String
  postedBy: User
}
}

type Query {
  info: String!
  feed: [Link]!
}

type Mutation {
  post(url: String!, description: String!): Link
}
